import styled from 'styled-components'

interface LinkProps {
  dataSource: any
}

const Link = ({

}: LinkProps) => {
  return (
    <LinkContainer></LinkContainer>
  )
}

const LinkContainer = styled.div``

export default Link
